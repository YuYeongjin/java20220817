package p99Study.cafe;

public class cafe091702 {
	/*
	 while(n!=0){
         int iValue=0;
     	int max = Integer.MIN_VALUE;
 	    for(int i =0;i<works.length; i++){
 	        if(max<works[i]){
 	            max=works[i];
                 iValue=i;
 	        }
 	    }	
 	    if(works[iValue]!=0) {
         	works[iValue]-=1;
         }
         n--;    
     }               
     for(int k=0; k<works.length;k++){        	
     	answer += Math.pow(works[k],2);      
     }
     */
}
